import React from 'react';
import ReactDOM from 'react-dom';

export default class VisualizedData extends React.Component {
	render() {
		return (
      <div>
        <span>VisualizedData</span>
      </div>
		);
	}
}
