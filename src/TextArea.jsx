import React from 'react';
import ReactDOM from 'react-dom';

export default class TextArea extends React.Component {
	render() {
		return (
      <div>
        <textarea className="json-text"></textarea>
      </div>
		);
	}
}
